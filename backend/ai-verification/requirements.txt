# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0          # ASGI server with websocket + reload support
python-multipart==0.0.9            # Required for file upload (Form / UploadFile)

# ── Configuration & Environment ────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.7.1                    # Data validation (built into FastAPI)
pydantic-settings==2.2.1           # Settings management via .env

# ── Async HTTP client ──────────────────────────────────────────────────────────
httpx==0.27.0                      # Async HTTP — download repos, call Pinata, oracle

# ── Task Queue & Caching ───────────────────────────────────────────────────────
celery==5.3.6                      # Async/background job processing
redis==5.0.4                       # Celery broker + result backend
flower==2.0.1                      # Celery monitoring dashboard (optional, useful)

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30                 # ORM — swap SQLite ↔ PostgreSQL with one URL change
alembic==1.13.1                    # Database migrations
aiosqlite==0.20.0                  # Async SQLite driver for prototype
asyncpg==0.29.0                    # Async PostgreSQL driver for production

# ── Static Analysis & Testing Tools ───────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6             # async test support
pytest-cov==5.0.0                  # coverage reports
pylint==3.2.0
flake8==7.0.0
flake8-bugbear==24.4.26            # extra flake8 rules
radon==6.0.1                       # cyclomatic complexity metrics
bandit==1.7.8                      # security linting

# ── Docker SDK ─────────────────────────────────────────────────────────────────
docker==7.0.0                      # Python Docker SDK — sandbox code execution

# ── AI / ML ────────────────────────────────────────────────────────────────────
sentence-transformers==2.7.0       # Semantic similarity for requirement matching
torch==2.3.0                       # PyTorch backend (CPU build is fine for prototype)
transformers==4.41.1               # HuggingFace model hub access
huggingface-hub==0.23.1            # Model downloads

# ── Blockchain / Web3 ──────────────────────────────────────────────────────────
web3==7.2.0                       # Interact with Sepolia — read events, send txns
eth-account==0.11.2                # Sign oracle results with private key

# ── IPFS / Storage ─────────────────────────────────────────────────────────────
pinatapy-vourhey==0.1.1            # Pinata IPFS upload wrapper (free tier)
# Alternative: use httpx directly against Pinata REST API (no extra dep needed)

# ── Utilities ──────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT tokens (for future auth layer)
passlib[bcrypt]==1.7.4             # Password hashing (future auth)
aiofiles==23.2.1                   # Async file I/O
python-dateutil==2.9.0             # Date parsing helpers
structlog==24.1.0                  # Structured JSON logging
tenacity==8.3.0                    # Retry logic for oracle + Pinata calls
rich==13.7.1                       # Pretty CLI output for scripts
gitpython==3.1.43                  # Clone / inspect GitHub repos locally

#LLM Intergration
openai 
anthropic 
